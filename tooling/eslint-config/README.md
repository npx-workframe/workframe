# ESLint Config

Shared lint and boundary rule package.

This slot owns import direction, cycle detection, server-only/client-only, and package boundary rules.

Boundaries that are not linted become folklore.
