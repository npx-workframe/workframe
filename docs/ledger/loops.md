# Workframe loops

The product loop is `observe → select → act → verify → record → stop`.
Architectonic Rail supplies the shared lifecycle; Workframe's harness, release
skill, dogfood environment, and source boundaries supply project-specific proof
and authority. Schedulers and roles are replaceable consumers of the same
ledger. They do not own parallel queues or reporter files.
