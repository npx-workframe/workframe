# Gate run — close Stages A→D to the E stop line

**Created:** 2026-07-08 (Fable planning session, supervised by Alan)
**Executor:** Cursor (pm-workframe + lane agents)
**Status truth:** [`backlog.json`](../backlog.json) → [`program-status.md`](../program-status.md)
**Discipline:** [`orchestration.md`](../orchestration.md) — verify-first, one WF per commit, no drive-by refactors, lanes stay in lane.

## Mission

Finish every non-deferred item in Stages A–D, then **stop at the E gate**. Do not start any Stage E+ item: WF-013, WF-014, WF-015, WF-029, WF-030, WF-NS-P3…P8 stay `deferred`.

**Gate reached when:**

1. `backlog.json` shows zero `todo`/`partial` in Stages A–D (WF-SK-001 meta may remain open).
2. Full verify matrix green at HEAD (`latest-verify-matrix.json`, no unexplained fails — run with sync+bundle prep, not `--skip-prep`).
3. No Stage E item touched.

## Verified baseline (2026-07-08)

- Backlog: 50 items — 29 done · 8 partial · 2 todo · 11 deferred. `ledger-next` → **WF-032**.
- `server.py`: **12,144 lines / 318 top-level defs** (backlog item summary still says ~20k — stale; refresh it when working WF-032).
- Run ledger: all six surfaces record — chat (`chat_stream.py`), mention, kanban (`kanban_cron.py`), cron/webhook/slash (`run_surface_wiring.py`). `test_run_ledger.py` + `test_run_surface_wiring.py` green this session.
- Verify matrix stale: 2026-07-06 @ `9a06d88`, 16/1/2 — fail is `ui_bundle_identity` (stale packed UI without sync+bundle prep).
- main in sync with origin. Dirty tree = cosmetic UI lane WIP (legit, see [`cosmetic-ui-lane.md`](cosmetic-ui-lane.md)) **plus two stray backend fixes** (see G0.1).
- Stashes: `cosmetic-ui-wip` (lane-d-authority, 07-06), `wf037-verify` (main). Local branches: `lane-d-authority` (merged), `ui/cosmetic-wf036`, `ui/restore-cosmetic-session`.

## Waves (ordered)

### Wave 0 — hygiene (do first, ~1 session)

| # | Task | Verify | Done means |
|---|------|--------|-----------|
| G0.1 | Commit the two stray backend-lane fixes sitting uncommitted on main: (a) `packages/create-workframe/bin/create-workframe.js` supervisor volume `→ /compose` (aligns generated compose with canonical `infra/compose/workframe` + `updates.py` contract); (b) `scripts/workframe/install-gate.ps1` `pnpm build:web` failure guard | `node scripts/workframe/run-negative-install-evidence.mjs`; scaffold test | Two surgical commits on main; UI-lane CSS files left alone |
| G0.2 | Refresh full verify matrix at HEAD **with prep** (sync + bundle before pack evidence) | all checks in `.harness/verify.mjs` + matrix runner | `latest-verify-matrix.json` at current SHA; `ui_bundle_identity` passes or fail is re-diagnosed |
| G0.3 | Branch/stash hygiene: confirm `lane-d-authority` fully merged then delete; inspect both stashes — drop only if content is superseded by committed work (check first, keep if in doubt) | `git branch --merged`, `git stash show -p` | No stale branches/stashes silently rotting |

### Wave 1 — close the cheap P1 partials (verify-first; expect backlog-only flips)

| # | Item | Known gap | Action |
|---|------|-----------|--------|
| G1.1 | **WF-NS-P2** (P1) | Only unverified acceptance sliver: DM chat records a run (DMs are rooms → chat surface; no explicit test) | Add/point a test or trace evidence that a DM turn inserts a run; then flip `done` + evidence + `completed_at`. Everything else verified 2026-07-08: tables, all six surfaces, activity panel reads `run_events` |
| G1.2 | **WF-NS-P1** (P1) | "Tests prove socket boundary" — no explicit SECURE_MODE socket-boundary test found in tree | Add negative test: workframe-api container cannot reach the Docker socket in SECURE_MODE (or cite WF-011 supervisor negatives + compose config assertion if they already prove it). Then flip with evidence |
| G1.3 | **WF-007** (P1) | `evaluate_open` wired in `updates.apply_update`; acceptance says update/**connect/adopt** blocked without authority. CLI wire already deferred to WF-014 | Verify whether connect/adopt paths exist in the shipped wedge. If they don't ship yet, record that in evidence and flip; if they exist, wire the smallest authority check |
| G1.4 | **WF-035** (P2) | Acceptance is scoped to auth/vault/provider-sync paths — `_log_handler_error` landed there 07-06; `test_exception_hygiene.py` exists. 73 broad `except Exception` remain in server.py but are out of acceptance scope | Run the test, verify path coverage, flip if met. Remaining broad excepts ride along WF-032 extractions only — no standalone sweep |

### Wave 2 — WF-032 server.py split (the bulk of the run)

- **G2.1** Refresh the module map: item summary/acceptance reference the 2026-07-05 audit (~20k lines) — most named modules are already extracted (~30 modules exist). Re-map the remaining 12.1k lines; dense zones: L2000–8000 credential/provider-binding + oauth/mention helpers (~220 defs), route-handler class body, runtime-token block (L10000–12000). Update the backlog item's summary/acceptance to the new map in the same commit as the first extraction.
- **G2.2** Extract one module per commit; after each: `py_compile`, `pnpm --filter @workframe/workframe-api typecheck`, `test_*.py` green. Candidate seams (verify before cutting): `provider_bindings.py`, `oauth_pending.py`, `runtime_tokens.py`, `mention_helpers.py`.
- **G2.3** Target: `server.py` residual **< 3k lines** (main + wiring + handler shell). Exception hygiene (WF-035 leftovers) is narrowed only inside modules being extracted.

### Wave 3 — release identity + funding slice

- **G3.1 WF-012** (P1 todo): expose package-version/build stamp in generated install (UI and/or API); PackageTruthGate asserts stamp == packed version. This kills the recurring `ui_bundle_identity` stale-bundle fail class. Backend + package lane (not cosmetic).
- **G3.2 WF-016** (P2) D-gate slice: emit a **receipt per run** (line-item record on `run_line_items`; amounts may be null). Payer enforcement already green (`test_run_authority.py`). Billing **amounts/pricing stay deferred** (E territory) — write that boundary into the item notes when flipping to done-for-D or keeping partial with a precise remainder.

### Wave 4 — production-cell evidence (operator-assisted)

- **G4.1 WF-017** (P2): run `verify-public-deploy.sh` green on the Hetzner reference VPS (slot 2, `open-vps-tunnel.ps1`), then document **and test** a backup/restore cycle of a live cell. Cursor preps scripts + docs; the VPS run and restore drill need Alan in the loop. This is the promotion gate for the first real team cell — do not mark done on script existence alone.

### Wave 5 — WF-036 functional slice (UI-functional lane, distinct from cosmetic)

- **G5.1** Verify first — composer model badge without session bind and `connectError` surfacing are already fixed per handoff; don't redo. Remaining acceptance: split `ConciergeFlow.tsx` (51 KB) and `HermesSessionContext.tsx` (29 KB) to <~800-line components in touched areas; one provider-label helper everywhere (`billingProviderDisplayLabel`); unknown model prefix renders model-only label (remove `inferProviderFromModelId` silent openrouter default). Coordinate with cosmetic lane — `HermesSessionContext.tsx` is on the cosmetic lane's off-limits list, so this slice belongs to the backend/functional agent.

### Meta (anytime, lowest priority)

- **WF-SK-001**: spec-kit mapping doc — docs-only; not gating.

## Lane rules for this run

- Backend lane: everything above except G5 cosmetic aspects. No cosmetic CSS sweeps.
- Cosmetic UI lane continues in parallel per [`cosmetic-ui-lane.md`](cosmetic-ui-lane.md) — unchanged.
- Status flips require run evidence (command + output), recorded on the backlog item; `completed_at` set at flip time.
- Append one line per outcome to `operations/log.md` (newest first).
- After each wave: re-run `node scripts/workframe/ledger-next.mjs` and sanity-check the pick against this plan; update `program-status.md` at wave boundaries, not per-commit.

## Explicitly out of scope (E gate — do not start)

Runtime adapters/`runtime_kind` migration (WF-014/015, NS-P4), tool-broker generalization (WF-029, NS-P3), Agent Vault spike (WF-030), file manifests (NS-P5), cell productization/control plane/marketplace (NS-P6…P8), billing amounts/pricing, `packages/workframe-integrations` BFF wiring.
