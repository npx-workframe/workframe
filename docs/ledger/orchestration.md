# Workframe orchestration

One claimed `WF-*` item may have one active owner. Parallel work is safe only on
distinct items and non-conflicting source boundaries. Pull/rebase before broad
changes, keep commits surgical, and never flip status without evidence. Backend
and UI roles follow the item scope; the ledger claim—not a handoff file—owns the
coordination boundary.
