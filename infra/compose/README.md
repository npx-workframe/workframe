# Compose

Local container orchestration and the simplest production-like runtime shape.

Use this slot for Postgres, Redis/Valkey, object storage, mail testing, API, worker, and supporting services when needed.

Carry one primary deployment target at a time. Avoid documenting contradictory deployment defaults as active choices.
