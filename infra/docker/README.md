# Docker

Dockerfile slot for deployable services.

Use this directory for API, worker, web, and service Dockerfiles once those surfaces are implemented.

Do not add contradictory deployment assumptions here. Dockerfiles should support the selected deployment path.
