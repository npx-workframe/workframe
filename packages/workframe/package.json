{
  "name": "workframe",
  "version": "0.2.2",
  "description": "Adaptive local entrypoint for Workframe runtimes and model providers.",
  "type": "module",
  "bin": {
    "workframe": "bin/workframe-cli.js"
  },
  "files": [
    "bin",
    "README.md"
  ],
  "scripts": {
    "check": "node --check bin/workframe.js && node --check bin/workframe-cli.js && node --check bin/origin-start.js",
    "test": "npm run check && node scripts/test-origin-start.mjs && node bin/workframe-cli.js status --json"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/npx-workframe/workframe.git"
  },
  "homepage": "https://workfra.me",
  "keywords": [
    "workframe",
    "agents",
    "hermes",
    "codex",
    "claude",
    "runtime",
    "diagnostics"
  ]
}
