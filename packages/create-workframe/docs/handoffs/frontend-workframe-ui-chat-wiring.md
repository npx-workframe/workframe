# Workframe UI Chat Wiring Notes

This file is now a compact historical note.

The canonical UI handoff is:

- `handoffs/frontend-workframe-ui.md`

Reason:

- the older chat-wiring writeup drifted out of date
- the current UI status is better represented by the live code in `packages/workframe-ui/` and `workframe-api/server.py`
- keeping one canonical handoff is simpler and lower-risk than maintaining two overlapping narratives

Use this note only as a pointer, not as the source of truth.
