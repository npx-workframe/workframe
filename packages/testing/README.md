# Testing

Shared fixtures, factories, mocks, test database helpers, and integration utilities.

This package is dev-only. Production code importing it is a boundary violation.

Keep test helpers boring and explicit. Do not hide product behavior inside fixtures.
