# UI

Shared owned UI primitives, blocks, tokens, and styles.

Use this package for components that are genuinely shared across web-like surfaces.

Do not put product rules here. UI may render state and trigger actions, but backend authority and domain invariants live elsewhere.
