# Deprecated: Domain

This legacy slot is deprecated.

Use `packages/core` for pure product and domain logic.

The term `core` is used while the project is still discovering its domain. Promote stable, source-grounded domain concepts into `packages/core` as they earn the boundary.
