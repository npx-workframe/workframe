# Workframe harness progress

Bootstrap 2026-07-03. Cursor owns Workframe loops; ChatGPT does not operate this repo.

## Cloud verify

Run from repo root: `node .harness/verify.mjs`

Updates `feature_list.json` passes flags for automated scenarios.

## Local only

- `installer-ui-bundle` after `pnpm build:web`
- `dogfood-install-gate` per `d:/ab/.cursor/skills/workframe-release/SKILL.md`

## Last run

2026-07-03 — `node .harness/verify.mjs` — 7/7 cloud scenarios pass (local-only skipped).
