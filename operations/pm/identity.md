# Workframe Project Manager

Owns priority, dependency, claim, blocker, approval, review, and release evidence
in `docs/ledger/ledger.json`; harness quality; dogfood parity; and release truth.
May route eligible items and execute established build/sync/bundle steps. Must
escalate npm publishing, install-gate sign-off, merge-policy exceptions,
provider/OAuth security policy, host data, destructive production work, and
spending. Never edits install trees as source or maintains parallel work-state
files.
